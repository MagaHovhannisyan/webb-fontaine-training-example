package information;

public class i1Git {
    /*
    BASIC GIT COMMANDS:
    -add ($ git add) - adding the file to your local repository. Unless the add command is performed, file has ?? status
    -commit ($ git commit -m "Add existing file") - committing the file in your local repository.
    -push ($ git push origin branch-name) - pushing the changes in your local repository to GitHub.

    HOW TO INTEGRATE YOUR PROJECT TO GIT:
    VCS -> Enable Version Control Integration -> Choose Git

    File STATUSES (checking command: git status -s ):
    -?? -> not git file
    -A -> added to git
    -M -> modified
    after git commit there is no status available. The version is created.
     */
}